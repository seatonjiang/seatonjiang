main:
  push:
    - name: 同步仓库到 GitHub
      imports:
        - https://cnb.cool/seatonjiang/secret-key/-/blob/main/github.yaml
      stages:
        - name: 同步仓库
          image: tencentcom/git-sync
          settings:
            target_url: https://github.com/seatonjiang/seatonjiang.git
            auth_type: https
            username: ${GITHUB_USERNAME}
            password: ${GITHUB_ACCESS_TOKEN}
